# nina-tts-alert
A Node.js "Server" that fetches official alert messages (from the BKK, api: NINA), converts them into speech, and saves the output as output.mp3. The generated audio can be played through an intercom or speaker system to broadcast warnings.
